@model IEnumerable<Servicely.Models.HealthCare_Type>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.healthcare_type_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.healthcare_isDeleted)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.healthcare_type_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.healthcare_isDeleted)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.healthcare_type_id }) |
                @Html.ActionLink("Details", "Details", new { id=item.healthcare_type_id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.healthcare_type_id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
