@model Servicely.Models.PoliceDepartment

@{
    ViewBag.Title = "Details";
       if (Session["Layout"] != null)
    { Layout = Session["Layout"].ToString(); }

    else
    { Layout = "~/Views/Shared/_Layout.cshtml"; }
}

<h2>Details</h2>

<div>
    <h4>PoliceDepartment</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.police_department_isDeleted)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.police_department_isDeleted)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.police_department_name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.police_department_name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.District.district_name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.District.district_name)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.police_department_id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
