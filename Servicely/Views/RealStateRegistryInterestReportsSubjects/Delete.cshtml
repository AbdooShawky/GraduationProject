@model Servicely.Models.RealStateRegistryInterestReportsSubject

@{
    ViewBag.Title = "Delete";
       if (Session["Layout"] != null)
    { Layout = Session["Layout"].ToString(); }

    else
    { Layout = "~/Views/Shared/_Layout.cshtml"; }
}

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Real State Registry Interest Reports Subject Name</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            <label>Real State Registry Interest Reports Subject Name</label>
        </dt>

        <dd>
            @Html.DisplayFor(model => model.realStateRegistryInterestReports_subject_name)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
