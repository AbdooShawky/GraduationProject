@model Servicely.Models.RealStateRegistryInterestReportsSubject

@{
    ViewBag.Title = "Edit";
        if (Session["Layout"] != null)
    { Layout = Session["Layout"].ToString(); }

    else
    { Layout = "~/Views/Shared/_Layout.cshtml"; }
}


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Real State Registry Interest Reports Subject</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.realStateRegistryInterestReports_subject_id)

        <div class="form-group">
            <label>Reports Subject Name</label>
            <div class="col-md-10">
                @Html.EditorFor(model => model.realStateRegistryInterestReports_subject_name, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.realStateRegistryInterestReports_subject_name, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
