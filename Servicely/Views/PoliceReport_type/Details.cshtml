@model Servicely.Models.PoliceReport_type

@{
    ViewBag.Title = "Details";
       if (Session["Layout"] != null)
    { Layout = Session["Layout"].ToString(); }

    else
    { Layout = "~/Views/Shared/_Layout.cshtml"; }
}

<h2>Details</h2>

<div>
    <h4>PoliceReport_type</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.police_report_type_name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.police_report_type_name)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.police_report_type_id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
